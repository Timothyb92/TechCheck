{
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.6.1",
    "prettier-plugin-tailwindcss": "^0.6.13"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"sleep 0.5 && npm run dev --prefix frontend\"",
    "format": "prettier \"**/*.{js,ts,tsx,json,css,md}\" --write"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
